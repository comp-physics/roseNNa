[project]
name = "rosenna"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["onnx>=1.14", "numpy>=1.24", "onnxruntime>=1.15"]

[project.scripts]
rosenna = "rosenna.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
